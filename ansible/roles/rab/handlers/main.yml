---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart RAB audit timer
  ansible.builtin.systemd_service:
    name: rab-audit.timer
    state: restarted
    daemon_reload: true

